##Git
    git pull origin main --allow-unrelated-histories
    git add .
    git commit -m "text"
    git push -u origin main



#name = "rafael"
#schools = []
#schools.append("Escola A")
#grades = [9.5, 8.0, 7.5] * 3
#print(grades)
#print(f"Nome: {name} is enrolled in {len(schools)} schools.")
